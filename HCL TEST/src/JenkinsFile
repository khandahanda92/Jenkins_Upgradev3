pipeline {
    agent any
    stages {
        stage('Build Sample Application') {
            steps {
                sh 'mvn -f HCL TEST/src/pom.xml clean package'
            }
            post {
                success {
                    echo "Now Archiving the Artifacts...."
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }
